// Modify the program to add two floating-point numbers and display the result.

#include<iostream>
using namespace std;

int main(){
    float a,b;
    cout << "Enter two floating-point numbers: ";
    cin >> a >> b;

    cout << "The sum of the two numbers is: " << a+b << endl;
}