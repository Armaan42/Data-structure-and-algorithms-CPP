#include<iostream>
using namespace std;

int main(){
    int n;
    cin >> n;

    // (n % 2 == 0) ? cout << "Even" : cout << "Odd" << endl;

    (n > 33) ? cout << "Pass" : cout << "Fail";
    return 0;
}